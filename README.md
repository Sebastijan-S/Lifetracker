# Life Tracker (PWA)

Serve the project over HTTP to test the PWA (service worker requires a secure context or localhost).

From the workspace root run:

```bash
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```
python3 -m http.server 8000